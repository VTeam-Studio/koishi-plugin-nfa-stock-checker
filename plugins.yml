plugins:
  nfa-stock-monitor:
    # API接口地址
    apiUrl: "https://mcheika.lol/user/api/index/stock"
    
    # 请求Cookie（可选，如不配置将使用默认值）
    # cookie: "你的Cookie值"
    
    # 商品ID
    itemId: "2"
    
    # 自定义消息模板（可选，使用 {stock} 作为库存数量占位符）
    # messageTemplate: "当前NFA库存: {stock}个"

